<div class="main-content">
  <div class="container">     
    <!-- inner page content -->
    <div class="inner-content">
		<%= form_for(@address, :url => myaccount_addresses_path(@address), html: {class: 'form-horizontal'}) do |f| %>
			<%= render :partial => 'form', :locals => {:f => f} %>
		<%end %>
		<div>
			<%= link_to 'Back to List', myaccount_addresses_path, :class => 'btn btn-success' %>
		</div>
		</div>
	</div>
</div>
