<div class="main-content">
  <div class="container">     
    <!-- inner page content -->
    <div class="inner-content">
		  <%= form_for(@form_address, :url => myaccount_address_path(@form_address), html: {class: 'form-horizontal'}) do |f| %>
		    <%= render :partial => 'form', :locals => {:f => f} %>
		  <% end %>
		  <div>
		    <%= link_to 'Back to Addresses', myaccount_addresses_path, :class => 'btn btn-success' %>
		  </div>
		</div>
	</div>
</div>
